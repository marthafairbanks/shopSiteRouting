(function() {
    'use strict';
    angular.module('shopSiteRouting').controller('MainController', 
    function(mainFactory) {

    	//add some arrays with data hardcoded in here to get pulled out

    	//have a different controller for each view
    });


})();